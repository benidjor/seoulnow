name: seoul
version: "0.1.0"
config-version: 2

profile: seoul_duckdb

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  seoul:
    staging:
      +materialized: view
    marts:
      +materialized: table
      +schema: gold
