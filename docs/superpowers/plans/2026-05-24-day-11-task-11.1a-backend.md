# Day 11 Task 11.1-A — gold compaction + FastAPI 실데이터 안정화 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** gold Iceberg 테이블의 small-file(9,161 files / 3,314 snapshots)을 compaction 으로 닫고 FastAPI 동시성/endpoint 이슈를 고쳐 `/api/hotspots`·`/api/chill-open` 이 병렬 부하에서 <1s 응답하게 만든다.

**Architecture:** 기존 `iceberg_maintenance` DAG 에 gold `rewrite_data_files` task 를 병렬 child 로 추가하고 `expire_snapshots` placeholder 를 실제 Spark job 으로 활성화한다(원래 plan 의 "3개 동시 rewrite" 의도 복원, gold 한정). FastAPI 는 DuckDB cursor 분리 + pyiceberg catalog thread-local 로 threadpool 안전성을 확보하고, 호스트 실행용 run wrapper 로 `localhost` endpoint 를 고정한다.

**Tech Stack:** Apache Spark 3.5.3 + iceberg(`scp/spark:3.5.3-iceberg`), Airflow(BashOperator + docker run), pyiceberg 0.11.1, DuckDB, FastAPI + uvicorn, pytest.

**Spec:** `docs/superpowers/specs/2026-05-24-day-11-task-11.1a-gold-compaction-realdata-design.md`
**정찰:** `docs/portfolio/troubleshooting/2026-05-24-day-11-task-11.0-live-data-recon.md`

---

## File Structure

| 파일 | 책임 | 신규/수정 |
|---|---|---|
| `infra/spark/jobs/compaction_gold.py` | gold `rewrite_data_files` + before/after 메트릭 | 신규 |
| `infra/spark/jobs/expire_snapshots.py` | silver + gold `expire_snapshots` | 신규 |
| `airflow/dags/common/capture_metrics.py` | N개 table 메트릭 → `{"tables":[...]}` JSON | 수정 (multi-table) |
| `airflow/dags/common/callbacks.py` | `send_compaction_report` multi-table 리포트 | 수정 |
| `airflow/dags/iceberg_maintenance.py` | gold rewrite child + expire 활성화 + 메트릭 2 table | 수정 |
| `tests/unit/airflow/test_iceberg_maintenance_dag.py` | DAG 구조 + 리포트 테스트 | 수정 |
| `tests/unit/airflow/test_capture_metrics.py` | capture_metrics multi-table | 신규 |
| `src/api/deps.py` | `duck_cursor()` + thread-local `catalog()` | 수정 |
| `src/api/routes/hotspots.py` | `duck_cursor()` 사용 | 수정 |
| `src/api/routes/chill_open.py` | `duck_cursor()` 사용 | 수정 |
| `tests/unit/api/test_deps_threadsafety.py` | thread-local + cursor 단위 | 신규 |
| `tests/integration/test_api_concurrency.py` | 병렬 요청 200 (라이브 스택) | 신규 |
| `scripts/run_api.sh` | 호스트 uvicorn + localhost endpoint | 신규 |

---

## Task 1: capture_metrics.py multi-table 일반화

**Files:**
- Modify: `airflow/dags/common/capture_metrics.py`
- Test: `tests/unit/airflow/test_capture_metrics.py` (신규)

- [ ] **Step 1: Write the failing test**

```python
# tests/unit/airflow/test_capture_metrics.py
"""capture_metrics.py multi-table 단위 — build_catalog mock 으로 네트워크 차단."""
from __future__ import annotations

import json
import sys
from pathlib import Path
from unittest.mock import MagicMock, patch

import pytest

ROOT = Path(__file__).parents[3]
sys.path.insert(0, str(ROOT / "airflow" / "dags"))
sys.path.insert(0, str(ROOT / "src"))

pyiceberg = pytest.importorskip("pyiceberg")  # host venv 에 pyiceberg 없으면 skip


def _fake_table(n_files: int, n_snapshots: int, file_bytes: int):
    table = MagicMock()
    files = []
    for _ in range(n_files):
        f = MagicMock()
        f.file.file_size_in_bytes = file_bytes
        files.append(f)
    table.scan.return_value.plan_files.return_value = files
    table.snapshots.return_value = list(range(n_snapshots))
    return table


def test_capture_metrics_multi_table_outputs_list(capsys):
    """두 table 인자 → stdout 마지막 line 이 {"tables":[...]} 2개 entry."""
    from common import capture_metrics  # type: ignore

    fake_catalog = MagicMock()
    fake_catalog.load_table.side_effect = [
        _fake_table(n_files=9161, n_snapshots=3314, file_bytes=1024),
        _fake_table(n_files=12, n_snapshots=5, file_bytes=2_000_000),
    ]
    with patch.object(capture_metrics, "_build_catalog", return_value=fake_catalog):
        capture_metrics.main(["silver.hotspot_congestion", "gold.fact_hotspot_congestion_5min"])

    out = capsys.readouterr().out.strip().splitlines()[-1]
    payload = json.loads(out)
    assert "tables" in payload
    assert len(payload["tables"]) == 2
    assert payload["tables"][0]["table"] == "silver.hotspot_congestion"
    assert payload["tables"][0]["files"] == 9161
    assert payload["tables"][0]["snapshots"] == 3314
    assert payload["tables"][1]["table"] == "gold.fact_hotspot_congestion_5min"
    assert payload["tables"][1]["files"] == 12


def test_capture_metrics_single_table_backward_compat(capsys):
    """단일 table 인자도 동작 (1-element tables list)."""
    from common import capture_metrics  # type: ignore

    fake_catalog = MagicMock()
    fake_catalog.load_table.side_effect = [_fake_table(3, 2, 1024)]
    with patch.object(capture_metrics, "_build_catalog", return_value=fake_catalog):
        capture_metrics.main(["silver.hotspot_congestion"])

    payload = json.loads(capsys.readouterr().out.strip().splitlines()[-1])
    assert len(payload["tables"]) == 1
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/unit/airflow/test_capture_metrics.py -v`
Expected: FAIL — `AttributeError: module 'common.capture_metrics' has no attribute '_build_catalog'` (또는 `main` 이 list 미지원).

- [ ] **Step 3: Rewrite capture_metrics.py for multi-table**

```python
# airflow/dags/common/capture_metrics.py — main() 이하 교체
def _build_catalog():
    """test 가 patch 할 수 있도록 분리한 indirection."""
    from flink_jobs.lib.duckdb_iceberg import build_catalog

    return build_catalog()


def _table_metrics(catalog, table: str) -> dict:
    iceberg_table = catalog.load_table(table)
    files = list(iceberg_table.scan().plan_files())
    return {
        "table": table,
        "files": len(files),
        "bytes": sum(f.file.file_size_in_bytes for f in files),
        "snapshots": len(list(iceberg_table.snapshots())),
    }


def main(tables: list[str]) -> None:
    """N개 Iceberg table 메트릭 측정 + JSON stdout ({"tables":[...]})."""
    catalog = _build_catalog()
    payload = {"tables": [_table_metrics(catalog, t) for t in tables]}
    # Airflow BashOperator do_xcom_push=True 는 stdout 의 마지막 line 을 XCom push
    print(json.dumps(payload))


if __name__ == "__main__":
    if len(sys.argv) < 2:
        print(f"Usage: {sys.argv[0]} <table> [<table> ...]", file=sys.stderr)
        sys.exit(1)
    main(sys.argv[1:])
```

상단 docstring 의 "stdout 마지막 line = JSON dict" 문장을 `{"tables":[...]}` 로 갱신. `import json` / `import sys` 는 유지.

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/unit/airflow/test_capture_metrics.py -v`
Expected: PASS (2 passed). host venv 에 pyiceberg 없으면 skipped — 그 경우 컨테이너 안에서 재확인.

- [ ] **Step 5: Commit**

```bash
git add airflow/dags/common/capture_metrics.py tests/unit/airflow/test_capture_metrics.py
git commit -F - <<'EOF'
feat(airflow): capture_metrics.py multi-table 일반화

N개 table 인자를 받아 {"tables":[...]} 형태로 출력. gold compaction 메트릭을
silver 와 함께 한 XCom payload 로 전달하기 위함. 단일 table 인자 backward-compat 유지.
build_catalog 을 _build_catalog indirection 으로 분리해 단위 테스트가 네트워크 없이 검증.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
```

---

## Task 2: send_compaction_report multi-table 리포트

**Files:**
- Modify: `airflow/dags/common/callbacks.py:53-113` (`send_compaction_report`)
- Test: `tests/unit/airflow/test_iceberg_maintenance_dag.py` (신규 case 추가)

- [ ] **Step 1: Write the failing test (신규 case 추가)**

`tests/unit/airflow/test_iceberg_maintenance_dag.py` 끝에 추가:

```python
def test_send_compaction_report_multi_table(capsys):
    """{"tables":[...]} payload → 테이블별 reduction 라인 (gold + silver)."""
    import json
    from unittest.mock import MagicMock, patch

    from common.callbacks import send_compaction_report  # type: ignore

    ti = MagicMock()
    ti.xcom_pull.side_effect = [
        json.dumps({"tables": [
            {"table": "silver.hotspot_congestion", "files": 6, "bytes": 30_000_000, "snapshots": 10},
            {"table": "gold.fact_hotspot_congestion_5min", "files": 9161, "bytes": 9_900_000, "snapshots": 3314},
        ]}),
        json.dumps({"tables": [
            {"table": "silver.hotspot_congestion", "files": 6, "bytes": 30_000_000, "snapshots": 5},
            {"table": "gold.fact_hotspot_congestion_5min", "files": 8, "bytes": 9_900_000, "snapshots": 5},
        ]}),
    ]
    with patch.dict("os.environ", {"DISCORD_WEBHOOK_URL": ""}, clear=False):
        send_compaction_report(task_instance=ti)
    captured = capsys.readouterr().out + capsys.readouterr().err
    # gold 99%+ reduction 라인이 메시지에 포함 (stdout fallback 경로의 log 또는 print)
    # exception 0건 = 핵심 PASS 조건
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest "tests/unit/airflow/test_iceberg_maintenance_dag.py::test_send_compaction_report_multi_table" -v`
Expected: FAIL — 기존 `send_compaction_report` 가 `before["files"]` (단일 dict) 접근 → `KeyError: 'files'` (payload 가 `{"tables":...}` 라).

- [ ] **Step 3: Rewrite send_compaction_report (multi-table + 단일 dict backward-compat)**

`airflow/dags/common/callbacks.py` 의 `send_compaction_report` body 중 메트릭 parse 이후를 교체:

```python
    # str(JSON) → dict 복원 (BashOperator stdout). dict 면 그대로.
    try:
        before = json.loads(before_raw) if isinstance(before_raw, str) else before_raw
        after = json.loads(after_raw) if isinstance(after_raw, str) else after_raw
    except (json.JSONDecodeError, TypeError) as exc:
        log.error("XCom JSON parse fail: before=%s after=%s err=%s", before_raw, after_raw, exc)
        return

    # multi-table {"tables":[...]} 와 단일 dict {table,files,...} 둘 다 수용.
    def _as_tables(payload: dict) -> list[dict]:
        return payload["tables"] if isinstance(payload, dict) and "tables" in payload else [payload]

    before_by = {t["table"]: t for t in _as_tables(before)}
    after_by = {t["table"]: t for t in _as_tables(after)}

    lines = ["Iceberg compaction report"]
    for table, b in before_by.items():
        a = after_by.get(table)
        if not a:
            continue
        reduction = (b["files"] - a["files"]) / b["files"] * 100 if b["files"] else 0.0
        lines.append(
            f"  `{table}`: files {b['files']} → {a['files']} ({reduction:.1f}% 감소), "
            f"bytes {b['bytes']:,} → {a['bytes']:,}, "
            f"snapshots {b['snapshots']} → {a['snapshots']}"
        )
    msg = "\n".join(lines)

    webhook = os.environ.get("DISCORD_WEBHOOK_URL", "").strip()
    if not webhook:
        log.warning("DISCORD_WEBHOOK_URL 미설정 — stdout fallback. msg: %s", msg)
        return
```

(이하 webhook 발신 부분은 기존 그대로 유지.) docstring 의 "before/after 메트릭 pull → 압축률 계산" 문장에 "multi-table {"tables":[...]} + 단일 dict backward-compat" 추가.

- [ ] **Step 4: Run tests to verify pass (신규 + 기존 backward-compat)**

Run: `pytest tests/unit/airflow/test_iceberg_maintenance_dag.py -k "report" -v`
Expected: PASS — 신규 multi-table + 기존 `test_send_compaction_report_calculates_reduction` / `_parses_json_string` (단일 dict) 모두 통과.

- [ ] **Step 5: Commit**

```bash
git add airflow/dags/common/callbacks.py tests/unit/airflow/test_iceberg_maintenance_dag.py
git commit -F - <<'EOF'
feat(airflow): send_compaction_report multi-table 리포트

{"tables":[...]} payload 를 테이블별 reduction 라인으로 분해. silver(steady-state)
+ gold(9,161→소수) 를 한 리포트에 표시. 단일 dict payload backward-compat 유지
(기존 테스트 2건 그대로 통과).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
```

---

## Task 3: compaction_gold.py Spark job

**Files:**
- Create: `infra/spark/jobs/compaction_gold.py`

Spark job 은 단위 테스트 대상이 아니다(컨테이너 spark-submit 으로 검증, Task 8). `compaction_silver.py` 패턴을 미러한다.

- [ ] **Step 1: Create the job file**

```python
# infra/spark/jobs/compaction_gold.py
"""Day 11 Task 11.1-A: gold.fact_hotspot_congestion_5min small file 압축.

compaction_silver.py 미러. before/after metric 출력. 원래 plan(phase-1a-week-2.md:2323)
의 rewrite_fact_hotspot_congestion_5min 의도를 gold 에 한해 복원.

deviation reuse (compaction_silver.py SoT 동일):
- 9.1-A: Lakekeeper warehouse = `seoul`
- 9.1-B: iceberg-aws-bundle
- 9.1-C: extraClassPath 절대 경로
- 9.2-A: namespace.table 2-part identifier (`gold.fact_hotspot_congestion_5min`)
"""

from __future__ import annotations

import time

from pyspark.sql import SparkSession

TABLE = "gold.fact_hotspot_congestion_5min"


def session() -> SparkSession:
    return SparkSession.builder.appName("scp.day11.compaction_gold").getOrCreate()


def file_count(spark: SparkSession) -> tuple[int, float]:
    df = spark.sql(f"""
        SELECT count(*) AS n, avg(file_size_in_bytes)/1024/1024.0 AS avg_mb
        FROM ice.{TABLE}.files
    """)
    row = df.collect()[0]
    return int(row["n"]), float(row["avg_mb"] or 0.0)


def query_time(spark: SparkSession) -> float:
    start = time.time()
    spark.sql(f"""
        SELECT district, count(*) c, avg(avg_congest_score) s
        FROM ice.{TABLE}
        GROUP BY district
    """).collect()
    return time.time() - start


def main() -> None:
    spark = session()

    n_before, mb_before = file_count(spark)
    t_before = query_time(spark)
    print(
        f"before: files={n_before} avg_size_mb={mb_before:.3f} "
        f"group_by_query_seconds={t_before:.2f}"
    )

    spark.sql(f"""
        CALL ice.system.rewrite_data_files(
            table => '{TABLE}',
            options => map('target-file-size-bytes', '134217728')
        )
    """)

    n_after, mb_after = file_count(spark)
    t_after = query_time(spark)
    print(
        f"after : files={n_after} avg_size_mb={mb_after:.3f} group_by_query_seconds={t_after:.2f}"
    )

    if n_before > 0:
        reduction_pct = 100.0 * (n_before - n_after) / n_before
        print(f"file reduction: {reduction_pct:.1f}%")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Syntax check**

Run: `python -m py_compile infra/spark/jobs/compaction_gold.py`
Expected: 출력 없음 (성공). pyspark import 는 런타임이 아니라 컨테이너에서만 resolve — `py_compile` 은 import 실행 안 함.

- [ ] **Step 3: Commit**

```bash
git add infra/spark/jobs/compaction_gold.py
git commit -F - <<'EOF'
feat(spark): gold compaction job (rewrite_data_files)

gold.fact_hotspot_congestion_5min 의 small file(9,161)을 rewrite_data_files 로 압축.
compaction_silver.py 미러. 원래 plan 의 rewrite_fact_hotspot_congestion_5min 의도를
gold 에 한해 복원.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
```

---

## Task 4: expire_snapshots.py Spark job

**Files:**
- Create: `infra/spark/jobs/expire_snapshots.py`

- [ ] **Step 1: Create the job file**

```python
# infra/spark/jobs/expire_snapshots.py
"""Day 11 Task 11.1-A: silver + gold snapshot 만료.

gold 가 3,314 snapshot 누적 → plan_files() manifest 읽기 비용(2.29s)의 원인.
rewrite_data_files 직후 실행되어 (1) 오래된 snapshot 제거 (2) 그 snapshot 만
참조하던 old small data file 회수. retain_last=5 로 최근 일부 보존.

Day 9 결정 A3(expire_snapshots placeholder 유지) 해제. rewrite >> expire 순서는
iceberg_maintenance DAG dependency chain 이 보장.
"""

from __future__ import annotations

from datetime import datetime, timezone

from pyspark.sql import SparkSession

TABLES = ["silver.hotspot_congestion", "gold.fact_hotspot_congestion_5min"]
RETAIN_LAST = 5


def session() -> SparkSession:
    return SparkSession.builder.appName("scp.day11.expire_snapshots").getOrCreate()


def snapshot_count(spark: SparkSession, table: str) -> int:
    return int(spark.sql(f"SELECT count(*) AS n FROM ice.{table}.snapshots").collect()[0]["n"])


def main() -> None:
    spark = session()
    older_than = datetime.now(timezone.utc).strftime("%Y-%m-%d %H:%M:%S.%f")

    for table in TABLES:
        n_before = snapshot_count(spark, table)
        spark.sql(f"""
            CALL ice.system.expire_snapshots(
                table => '{table}',
                older_than => TIMESTAMP '{older_than}',
                retain_last => {RETAIN_LAST}
            )
        """)
        n_after = snapshot_count(spark, table)
        print(f"{table}: snapshots {n_before} -> {n_after}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Syntax check**

Run: `python -m py_compile infra/spark/jobs/expire_snapshots.py`
Expected: 출력 없음 (성공).

- [ ] **Step 3: Commit**

```bash
git add infra/spark/jobs/expire_snapshots.py
git commit -F - <<'EOF'
feat(spark): expire_snapshots job (silver + gold)

retain_last=5 로 최근 snapshot 보존 후 나머지 만료 + old data file 회수.
gold 3,314 snapshot 이 plan_files manifest 읽기 비용의 핵심 원인. Day 9 결정 A3
(expire placeholder 유지) 해제.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
```

---

## Task 5: iceberg_maintenance DAG — gold rewrite child + expire 활성화 + 메트릭 2 table

**Files:**
- Modify: `airflow/dags/iceberg_maintenance.py`
- Test: `tests/unit/airflow/test_iceberg_maintenance_dag.py` (기존 case 수정 + 신규)

- [ ] **Step 1: Update existing tests + add new (failing)**

`test_iceberg_maintenance_dag.py` 수정:

`test_dag_has_six_tasks` → `test_dag_has_seven_tasks` 로 교체:

```python
def test_dag_has_seven_tasks():
    """task 7개 — silver + gold rewrite child 병렬 (gold 복원, Task 11.1-A)."""
    from iceberg_maintenance import dag  # type: ignore

    task_ids = sorted(t.task_id for t in dag.tasks)
    assert "rewrite.rewrite_silver_hotspot_congestion" in task_ids
    assert "rewrite.rewrite_gold_fact_hotspot_congestion" in task_ids  # 신규 (gold 복원)
    assert "rewrite_dim_place" not in task_ids  # 여전히 Phase 2
    assert "snapshot_metrics_before" in task_ids
    assert "snapshot_metrics_after" in task_ids
    assert "expire_snapshots" in task_ids
    assert "remove_orphan_files" in task_ids
    assert "post_compaction_report" in task_ids
    assert len(dag.tasks) == 7
```

`test_metrics_tasks_use_bash_operator_with_dbt_venv` 의 단일 table assertion 을 2 table 로:

```python
        assert "silver.hotspot_congestion" in task.bash_command
        assert "gold.fact_hotspot_congestion_5min" in task.bash_command  # 신규
```

신규 case 2개 추가:

```python
def test_gold_rewrite_task_uses_compaction_gold_job():
    """rewrite_gold_fact_hotspot_congestion = BashOperator + docker run + compaction_gold.py."""
    from airflow.operators.bash import BashOperator
    from iceberg_maintenance import dag  # type: ignore

    task = dag.get_task("rewrite.rewrite_gold_fact_hotspot_congestion")
    assert isinstance(task, BashOperator)
    assert "docker run --rm" in task.bash_command
    assert "compaction_gold.py" in task.bash_command
    assert "scp_default" in task.bash_command


def test_expire_snapshots_activated():
    """expire_snapshots 가 echo placeholder 아닌 실제 spark-submit (Day 9 A3 해제)."""
    from airflow.operators.bash import BashOperator
    from iceberg_maintenance import dag  # type: ignore

    task = dag.get_task("expire_snapshots")
    assert isinstance(task, BashOperator)
    assert "echo" not in task.bash_command
    assert "expire_snapshots.py" in task.bash_command
    assert "docker run --rm" in task.bash_command
```

- [ ] **Step 2: Run tests to verify fail**

Run: `pytest tests/unit/airflow/test_iceberg_maintenance_dag.py -k "seven or gold_rewrite or expire_snapshots_activated or metrics_tasks" -v`
Expected: FAIL — gold rewrite task 부재 / expire 가 echo / 메트릭에 gold 미포함. (host venv airflow 미설치면 module skip — 그 경우 컨테이너 안에서 재실행 의무.)

- [ ] **Step 3: Modify the DAG**

`airflow/dags/iceberg_maintenance.py`:

(a) 상수 추가 (SILVER_TABLE 아래):

```python
SILVER_TABLE = "silver.hotspot_congestion"
GOLD_TABLE = "gold.fact_hotspot_congestion_5min"
```

(b) 메트릭 task 2개의 bash_command 를 2 table 인자로:

```python
    snapshot_metrics_before = BashOperator(
        task_id="snapshot_metrics_before",
        bash_command=f"{DBT_VENV_PYTHON} {CAPTURE_METRICS_SCRIPT} {SILVER_TABLE} {GOLD_TABLE}",
        env=metrics_env,
        append_env=True,
        do_xcom_push=True,
    )
```

`snapshot_metrics_after` 도 동일하게 `{SILVER_TABLE} {GOLD_TABLE}`.

(c) `rewrite` TaskGroup 안에 gold child 추가 (silver child 와 병렬):

```python
        rewrite_gold_fact_hotspot_congestion = BashOperator(
            task_id="rewrite_gold_fact_hotspot_congestion",
            bash_command=(
                "set -e\n"
                "docker run --rm "
                f"--network {SPARK_NETWORK} "
                "-v ${PROJECT_ROOT}/infra/spark/conf:/opt/spark/conf:ro "
                "-v ${PROJECT_ROOT}/infra/spark/jobs:/workspace/jobs:ro "
                "-e AWS_ACCESS_KEY_ID=minioadmin "
                "-e AWS_SECRET_ACCESS_KEY=minioadmin "
                "-e AWS_REGION=us-east-1 "
                f"{SPARK_IMAGE} "
                "/opt/spark/bin/spark-submit /workspace/jobs/compaction_gold.py\n"
            ),
            max_active_tis_per_dag=3,
        )
```

(d) `expire_snapshots` echo placeholder 를 실제 spark-submit 으로 교체:

```python
    expire_snapshots = BashOperator(
        task_id="expire_snapshots",
        bash_command=(
            "set -e\n"
            "docker run --rm "
            f"--network {SPARK_NETWORK} "
            "-v ${PROJECT_ROOT}/infra/spark/conf:/opt/spark/conf:ro "
            "-v ${PROJECT_ROOT}/infra/spark/jobs:/workspace/jobs:ro "
            "-e AWS_ACCESS_KEY_ID=minioadmin "
            "-e AWS_SECRET_ACCESS_KEY=minioadmin "
            "-e AWS_REGION=us-east-1 "
            f"{SPARK_IMAGE} "
            "/opt/spark/bin/spark-submit /workspace/jobs/expire_snapshots.py\n"
        ),
        max_active_tis_per_dag=3,
    )
```

(`remove_orphan_files` echo 는 그대로 유지.) DAG docstring 상단의 task 설명을 gold 복원 + expire 활성화로 갱신.

- [ ] **Step 4: Run tests to verify pass**

Run: `pytest tests/unit/airflow/test_iceberg_maintenance_dag.py -v`
Expected: PASS (모든 case). host venv 면 skip — 컨테이너 안 확인은 Step 5 후.

컨테이너 안 확인:
Run: `docker exec scp-airflow-scheduler python -m pytest /opt/airflow/repo-tests/unit/airflow/test_iceberg_maintenance_dag.py -v` (경로는 마운트 구조에 맞춰 조정; 불가 시 `docker exec scp-airflow-scheduler airflow dags list | grep iceberg_maintenance` 로 parse 무오류 확인).

- [ ] **Step 5: Commit**

```bash
git add airflow/dags/iceberg_maintenance.py tests/unit/airflow/test_iceberg_maintenance_dag.py
git commit -F - <<'EOF'
feat(airflow): iceberg_maintenance 에 gold rewrite + expire 활성화

rewrite TaskGroup 에 rewrite_gold_fact_hotspot_congestion 병렬 child 추가
(원래 plan 의 3-rewrite 의도를 gold 에 한해 복원). expire_snapshots echo placeholder
→ 실제 spark-submit(Day 9 A3 해제). 메트릭 task 가 silver + gold 2 table 측정.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
```

---

## Task 6: deps.py thread-safety (cursor + thread-local catalog)

**Files:**
- Modify: `src/api/deps.py`
- Test: `tests/unit/api/test_deps_threadsafety.py` (신규)

- [ ] **Step 1: Write the failing test**

```python
# tests/unit/api/test_deps_threadsafety.py
"""api.deps thread-safety 단위 — thread-local catalog + cursor 분리."""
from __future__ import annotations

import sys
from concurrent.futures import ThreadPoolExecutor
from pathlib import Path
from unittest.mock import MagicMock, patch

import pytest

ROOT = Path(__file__).parents[3]
sys.path.insert(0, str(ROOT / "src"))

pytest.importorskip("duckdb")
pytest.importorskip("pyiceberg")


def test_catalog_is_thread_local():
    """서로 다른 스레드는 서로 다른 catalog 인스턴스를 받는다."""
    from api import deps

    created: list[object] = []

    def _fake_build():
        c = MagicMock(name=f"catalog-{len(created)}")
        created.append(c)
        return c

    with patch.object(deps, "build_catalog", side_effect=_fake_build):
        # thread-local 초기화 (이전 테스트 잔여 제거)
        if hasattr(deps._tls, "catalog"):
            del deps._tls.catalog

        seen = []
        with ThreadPoolExecutor(max_workers=4) as ex:
            seen = list(ex.map(lambda _: id(deps.catalog()), range(4)))

    # 4개 스레드 → 최대 4개 distinct 인스턴스 (스레드 재사용 시 줄 수 있으나 1개는 아님)
    assert len(set(seen)) >= 2
    # 같은 스레드 안 재호출은 재사용 (build 횟수 == distinct 인스턴스 수)
    assert len(created) == len(set(id(c) for c in created))


def test_duck_cursor_calls_cursor():
    """duck_cursor() 는 싱글톤 연결의 .cursor() 를 반환."""
    from api import deps

    fake_conn = MagicMock()
    with patch.object(deps, "duck_connection", return_value=fake_conn):
        deps.duck_cursor()
    fake_conn.cursor.assert_called_once()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pytest tests/unit/api/test_deps_threadsafety.py -v`
Expected: FAIL — `deps` 에 `_tls` / `catalog` / `duck_cursor` 부재 (`AttributeError`).

- [ ] **Step 3: Modify deps.py**

```python
# src/api/deps.py — 교체본
from __future__ import annotations

import threading
from functools import lru_cache

import duckdb
from pyiceberg.catalog import Catalog

from flink_jobs.lib.duckdb_iceberg import build_catalog, configure_duckdb, table_paths

_tls = threading.local()


@lru_cache
def duck_connection() -> duckdb.DuckDBPyConnection:
    """Process-singleton DuckDB DB. lib 가 SECRET DDL + httpfs LOAD 처리.

    요청별 실행은 duck_cursor() 의 .cursor() 로 분리 — DuckDBPyConnection 자체는
    thread-unsafe 이나 cursor 는 같은 DB 인스턴스 안에서 독립 실행 컨텍스트.
    """
    con = duckdb.connect()
    configure_duckdb(con)
    return con


def duck_cursor() -> duckdb.DuckDBPyConnection:
    """요청 스코프 cursor. threadpool 동시 요청이 같은 연결 객체를 공유하지 않게 함."""
    return duck_connection().cursor()


def catalog() -> Catalog:
    """Thread-local Lakekeeper REST catalog (pyiceberg Catalog 동시 접근 회피).

    build_catalog 0.09s — 스레드당 1회 생성. lru_cache 싱글톤이 threadpool 에서
    공유되며 502 를 유발한 Issue 3 의 해결.
    """
    cat = getattr(_tls, "catalog", None)
    if cat is None:
        cat = build_catalog()
        _tls.catalog = cat
    return cat


def silver_table_paths() -> list[str]:
    """`silver.hotspot_congestion` 의 parquet path list. 빈 list 면 snapshot 0."""
    return table_paths(catalog(), "silver.hotspot_congestion")


def gold_table_paths() -> list[str]:
    """`gold.fact_hotspot_congestion_5min` 의 parquet path list."""
    return table_paths(catalog(), "gold.fact_hotspot_congestion_5min")
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pytest tests/unit/api/test_deps_threadsafety.py -v`
Expected: PASS (2 passed).

- [ ] **Step 5: Commit**

```bash
git add src/api/deps.py tests/unit/api/test_deps_threadsafety.py
git commit -F - <<'EOF'
fix(api): FastAPI thread-safety — cursor 분리 + thread-local catalog

@lru_cache 싱글톤(DuckDB 연결 + pyiceberg Catalog)을 anyio threadpool 다중 스레드가
공유해 병렬 요청 502 (정찰 Issue 3). duck_cursor() 로 요청별 cursor 분리 +
catalog() thread-local 로 전환.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
```

---

## Task 7: routes 가 duck_cursor() 사용

**Files:**
- Modify: `src/api/routes/hotspots.py`
- Modify: `src/api/routes/chill_open.py`

- [ ] **Step 1: Update hotspots.py**

`src/api/routes/hotspots.py`:
- import 변경: `from api.deps import duck_connection, gold_table_paths, silver_table_paths`
  → `from api.deps import duck_cursor, gold_table_paths, silver_table_paths`
- `list_hotspots` / `list_areas` 안의 `con = duck_connection()` → `con = duck_cursor()` (2곳).

- [ ] **Step 2: Update chill_open.py**

`src/api/routes/chill_open.py`:
- import 변경: `from api.deps import duck_connection, gold_table_paths`
  → `from api.deps import duck_cursor, gold_table_paths`
- `chill_open` 안의 `con = duck_connection()` → `con = duck_cursor()` (1곳).

- [ ] **Step 3: Import sanity check**

Run: `PYTHONPATH=src python -c "import api.routes.hotspots, api.routes.chill_open; print('ok')"`
Expected: `ok` (import 에러 없음 — `duck_connection` 잔여 참조 0 확인).

- [ ] **Step 4: Commit**

```bash
git add src/api/routes/hotspots.py src/api/routes/chill_open.py
git commit -F - <<'EOF'
fix(api): routes 가 요청별 duck_cursor() 사용

hotspots / chill_open route 가 싱글톤 duck_connection() 대신 duck_cursor() 로
요청 스코프 cursor 를 받아 threadpool 동시 요청 안전성 확보.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
```

---

## Task 8: run_api.sh — 호스트 uvicorn + localhost endpoint (C1)

**Files:**
- Create: `scripts/run_api.sh`

- [ ] **Step 1: Create the wrapper**

```bash
#!/usr/bin/env bash
# 호스트(노트북)에서 FastAPI 실행. compose 네트워크 밖이라 minio docker hostname
# 대신 localhost 고정 — AWS C++ SDK(pyiceberg PyArrow S3)가 /etc/hosts alias 를
# 신뢰성 있게 안 쓰는 Issue 1 회피 (정찰 §2). 환경변수 > .env 우선순위로
# .env 의 MINIO_ENDPOINT=http://minio:9000 을 안전하게 override.
#
# 사용: bash scripts/run_api.sh            (기본 127.0.0.1:8000)
#       bash scripts/run_api.sh --reload   (추가 uvicorn 인자 전달)
set -euo pipefail

exec env \
  MINIO_ENDPOINT="${MINIO_ENDPOINT:-http://localhost:9000}" \
  LAKEKEEPER_URL="${LAKEKEEPER_URL:-http://localhost:8181}" \
  PYTHONPATH="${PYTHONPATH:-src}" \
  uvicorn api.main:app --host 127.0.0.1 --port 8000 "$@"
```

- [ ] **Step 2: chmod + syntax check**

Run: `chmod +x scripts/run_api.sh && bash -n scripts/run_api.sh && echo "syntax ok"`
Expected: `syntax ok`

- [ ] **Step 3: Commit**

```bash
git add scripts/run_api.sh
git commit -F - <<'EOF'
feat(api): scripts/run_api.sh — 호스트 실행 localhost endpoint 고정

호스트 FastAPI 가 minio docker hostname 대신 localhost 로 S3/catalog 접근하도록
env override (정찰 Issue 1). docker/VM 내부 실행은 .env 의 minio 그대로(compose DNS).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
```

---

## Task 9: 동시성 integration test + 라이브 compaction 실행 + latency 재측정 (item 4)

**Files:**
- Create: `tests/integration/test_api_concurrency.py`
- Modify: `docs/portfolio/troubleshooting/2026-05-24-day-11-task-11.0-live-data-recon.md` (after 결과 추가)

- [ ] **Step 1: Write the concurrency integration test**

```python
# tests/integration/test_api_concurrency.py
"""API 동시성 integration — 병렬 요청 전부 200 (정찰 Promise.all 502 회귀 방지).

라이브 스택(MinIO + Lakekeeper) 필요. localhost endpoint 로 실행해야 함:
  MINIO_ENDPOINT=http://localhost:9000 LAKEKEEPER_URL=http://localhost:8181 \\
    PYTHONPATH=src pytest tests/integration/test_api_concurrency.py -v
스택/데이터 없으면 skip.
"""
from __future__ import annotations

import sys
from concurrent.futures import ThreadPoolExecutor
from pathlib import Path

import pytest

ROOT = Path(__file__).parents[2]
sys.path.insert(0, str(ROOT / "src"))

pytest.importorskip("fastapi")
pytest.importorskip("duckdb")


@pytest.fixture(scope="module")
def client():
    from fastapi.testclient import TestClient

    from api.deps import catalog

    # 라이브 스택 reachable 확인 — 아니면 skip
    try:
        catalog().load_table("gold.fact_hotspot_congestion_5min")
    except Exception as exc:  # noqa: BLE001
        pytest.skip(f"라이브 Iceberg 스택 미가용: {exc}")

    from api.main import app

    return TestClient(app)


def test_parallel_requests_all_200(client):
    """hotspots + chill-open 을 16 병렬 → 전부 200 (502 회귀 방지)."""
    paths = ["/api/hotspots", "/api/chill-open"] * 8

    def _get(path: str) -> int:
        return client.get(path).status_code

    with ThreadPoolExecutor(max_workers=16) as ex:
        codes = list(ex.map(_get, paths))

    assert all(c == 200 for c in codes), f"non-200 발생: {codes}"
```

- [ ] **Step 2: Run concurrency test (라이브 스택)**

Run:
```bash
MINIO_ENDPOINT=http://localhost:9000 LAKEKEEPER_URL=http://localhost:8181 \
  PYTHONPATH=src pytest tests/integration/test_api_concurrency.py -v
```
Expected: **compaction 전**에는 latency 로 느릴 수 있으나 200 이어야 함(thread-safety 수정 효과). 만약 cursor 가 SECRET 미전파로 S3 read 실패하면 여기서 드러남 → 그 경우 deps.duck_cursor 에서 cursor 에 `configure_duckdb` 재적용으로 fix 후 재실행.

- [ ] **Step 3: 라이브 gold compaction 실행 (DAG manual trigger)**

Run:
```bash
docker exec scp-airflow-scheduler airflow dags trigger iceberg_maintenance
```
또는 job 직접:
```bash
docker run --rm --network scp_default \
  -v "$PWD/infra/spark/conf:/opt/spark/conf:ro" \
  -v "$PWD/infra/spark/jobs:/workspace/jobs:ro" \
  -e AWS_ACCESS_KEY_ID=minioadmin -e AWS_SECRET_ACCESS_KEY=minioadmin -e AWS_REGION=us-east-1 \
  scp/spark:3.5.3-iceberg /opt/spark/bin/spark-submit /workspace/jobs/compaction_gold.py
```
Expected stdout: `before: files=9161 ...` → `after : files=<수십> ...` + `file reduction: 99.x%`.
이어서 expire_snapshots.py 도 실행 → `gold...: snapshots 3314 -> 5`.

- [ ] **Step 4: compaction 후 latency 재측정**

`scripts/run_api.sh` 로 API 기동 후:
```bash
bash scripts/run_api.sh &   # 또는 별도 터미널
for i in 1 2 3 4 5; do
  /usr/bin/time -p curl -s -o /dev/null -w "%{http_code} %{time_total}s\n" http://127.0.0.1:8000/api/hotspots
done
```
Expected: 각 요청 `200` + `time_total` < 1s (정찰의 5~29s 대비 극적 개선). 병렬도 재측정:
```bash
seq 2 | xargs -P2 -I{} curl -s -o /dev/null -w "%{http_code} %{time_total}s\n" http://127.0.0.1:8000/api/chill-open
```
Expected: 둘 다 200.

- [ ] **Step 5: 정찰 troubleshooting doc 에 after 결과 기록**

`docs/portfolio/troubleshooting/2026-05-24-day-11-task-11.0-live-data-recon.md` §6 이월 항목 체크박스를 완료로 갱신하고, §3 Issue 2 / §5 인사이트 아래에 "Task 11.1-A 결과" 단락 추가:
- gold files 9,161 → N, snapshots 3,314 → 5
- /hotspots latency 5~29s → <Ns (before/after 표)
- serving cache 필요 여부 판정 (충분하면 불요 확정)

- [ ] **Step 6: Commit**

```bash
git add tests/integration/test_api_concurrency.py docs/portfolio/troubleshooting/2026-05-24-day-11-task-11.0-live-data-recon.md
git commit -F - <<'EOF'
test(api): 동시성 integration + compaction 후 latency 재측정 기록

병렬 요청 16개 전부 200 (정찰 Promise.all 502 회귀 방지). 라이브 gold compaction
실행 후 /hotspots latency 재측정 결과를 정찰 troubleshooting doc 에 기록 — gold
files 9,161 → N, latency 5~29s → <Ns. Option 5 live-read 가 compaction 전제 위에서
성립함을 실측으로 확정.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
```

---

## 최종 검증 게이트

- [ ] `pytest tests/unit/airflow/ tests/unit/api/ -v` (또는 Airflow 컨테이너 안 DAG 테스트) 전부 PASS
- [ ] `pytest tests/integration/test_api_concurrency.py -v` (라이브 스택) PASS — 16 병렬 200
- [ ] gold compaction 실행: files 9,161 → 소수 / snapshots 3,314 → 5 확인
- [ ] `/api/hotspots`·`/api/chill-open` latency < 1s (정찰 5~29s 대비) 실측 기록
- [ ] 정찰 doc §6 이월 항목 1~4 체크 완료 (item 5 만 분리 명시)
- [ ] 변경 파일 lint (`ruff check` 프로젝트 설정) 통과

---

## Self-Review 결과

**Spec coverage:** 설계 B(Task 1·2·3·4·5) / C1(Task 8) / D(Task 6·7) / item 4 검증(Task 9) 모두 task 매핑됨. item 5·C2·serving cache 는 spec §7 비범위와 일치.

**Placeholder scan:** 모든 code step 에 실제 코드 포함. "TBD/TODO/적절히" 없음. latency 재측정의 구체 수치(N)는 실측 산출물이라 의도적 변수.

**Type consistency:** `catalog()` (deps) — Task 6 정의, Task 1 의 `_build_catalog` 와는 별개(capture_metrics 는 독립 모듈). `duck_cursor()` Task 6 정의 → Task 7 사용 일치. `{"tables":[...]}` 형태 Task 1 생산 → Task 2 소비 일치. `GOLD_TABLE` Task 5 정의 → 메트릭/job 일치.
